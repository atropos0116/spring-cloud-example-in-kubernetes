apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: deatil-postgres-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 32Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deatil-postgres
spec:
  selector:
    matchLabels:
      app: deatil-postgres
  template:
    metadata:
      labels:
        app: deatil-postgres
    spec:
      volumes:
        - name: deatil-postgres-storage
          persistentVolumeClaim:
            claimName: deatil-postgres-pv-claim
      containers:
        - image: postgres
          name: postgres
          env:
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: postgres-config
                  key: postgres_user
            - name: POSTGRES_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: postgres-config
                  key: postgres_password
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          ports:
            - containerPort: 5432
              name: deatil-postgres
          volumeMounts:
            - name: deatil-postgres-storage
              mountPath: /var/lib/postgresql/data
---
apiVersion: v1
kind: Service
metadata:
  name: deatil-postgres
spec:
  type: ClusterIP
  ports:
    - port: 5433
  selector:
    app: deatil-postgres
